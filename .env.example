# Used by examples/build_index.py --remote
HOST_NAME="your.sftp.host"
USER_NAME="your_username"
KEY_PATH="~/.ssh/id_rsa"

# Prefer ssh-agent for encrypted keys:
#   ssh-add "$KEY_PATH"
#
# Still available but not recommended (prefer ssh-agent):
# SSH_KEY_PASSWORD="your_ssh_key_passphrase"

# Optional extra options JSON merged into SFTP options:
# RADARBASE_STORAGE_OPTIONS={"port": 22}
